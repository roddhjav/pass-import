[client]
verbose = false
datadir = /tmp/sphinx/
timeout = 30
rwd_keys = true
threshold = 2
validate_password = false
# v1 server
address="localhost"
port=10000
delete_upgraded=true

[servers]
[servers.zero]
host="localhost"
port=10000

[servers.one]
host="localhost"
port=10001

[servers.two]
host="localhost"
port=10002
